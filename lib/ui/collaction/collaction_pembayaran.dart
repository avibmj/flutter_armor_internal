import 'package:flutter/material.dart';

class CollactionPembayaran extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("pembayaran")),
      body: Center(child: Text("Ini Halaman pembayaran")),
    );
  }
}
