import 'package:flutter/material.dart';

class CollactionKunjungan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("kunjungan")),
      body: Center(child: Text("Ini Halaman kunjungan")),
    );
  }
}
