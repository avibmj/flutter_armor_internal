import 'package:flutter/material.dart';

class BucketScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Bucket")),
      body: Center(child: Text("Ini Halaman Bucket")),
    );
  }
}
